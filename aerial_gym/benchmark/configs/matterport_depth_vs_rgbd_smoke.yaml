seed: 0

env_counts: [1, 2]
warmup_steps: 20
bench_steps: 40

headless: true
device: cuda:0
controller_name: lee_position_control

camera:
  width: 240
  height: 135
  max_range: 80.0
  enable_lighting: false

render:
  render_every: 1

gif:
  save_gifs: false
  every: 5
  max_frames: 200
  duration_ms: 60

scene:
  folder: 00800-TEEsavR23oF
  root: resources/envs/val
  file: null

spawn_region:
  from_scene_bounds: false

navmesh:
  enabled: null
  navmesh_file: null
  edge_padding: 0.5
  oversample_factor: 8
  max_resample_rounds: 12
  strict_edge_padding: true
  min_edge_padding_ratio: 0.35
  padding_relaxation_factor: 0.7
  max_padding_relax_rounds: 3
  enforce_env_bounds: true
  max_bound_resample_rounds: 6
  spawn_height_offset_range: [0.15, 0.4]
  zero_velocity_on_spawn: true

robots:
  depth_only: base_quadrotor_with_camera
  rgbd: base_quadrotor_with_rgbd_camera

output:
  report_path: aerial_gym/benchmark/stored_data/matterport_depth_vs_rgbd_benchmark_smoke.json
